import Head from "next/head";

export default function Home() {
  return (
      <main>
          <Head>
              <title>WebSocket App</title>
              <meta name="description" content="A simple Next.js app with WebSocket"/>
          </Head>
          <h1>Welcome to the WebSocket App</h1>
      </main>
  );
}
